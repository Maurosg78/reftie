# Copiar a .env (gitignored) y completar con una key real.
# Solo se usa en scripts/runExtractionEval.ts — nunca en tests de CI,
# que corren sin llamar a ningún LLM (ver docs/adr/0004).
ANTHROPIC_API_KEY=
